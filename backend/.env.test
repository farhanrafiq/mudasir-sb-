# Test Environment Variables
PORT=3002
NODE_ENV=test

# Database Configuration (use a test DB or mock)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=union_registry_test
DB_USER=postgres
DB_PASSWORD=test_password

# JWT Configuration
JWT_SECRET=test_jwt_secret
JWT_EXPIRES_IN=1h

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Admin Credentials
ADMIN_PASSWORD=TestAdmin@2025

# MongoDB Test URI
MONGO_URI=mongodb://localhost:27017/union_registry_test
